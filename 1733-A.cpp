#include <bits/stdc++.h>

using namespace std;

long long solve(){
    int n, k;
    vector<long long>vec(n);
    for(auto &i : vec){
        cin>>i;
    }
    for(int i=0;i<n-1;i++){
        int(j=i+1;j<n;j++){
            if(vec[i]%k)
        }
    }
}
